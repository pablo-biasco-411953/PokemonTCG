version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: pokefetch-db
    environment:
      MYSQL_ROOT_PASSWORD: "1234"
      MYSQL_DATABASE: pokemon_tcg
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
